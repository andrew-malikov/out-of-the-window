# Markdown sample

Window theme language preview checklist:

- **Keywords** and *types* should stay distinct.
- Inline `code` should be readable.
- Links should have clear contrast: [VS Code](https://code.visualstudio.com/).

## Code fence preview

```ts
const answer: number = 42;
console.log(`value=${answer}`);
```

> Blockquote styling helps verify muted foreground hierarchy.

1. Heading contrast
2. List marker visibility
3. Emphasis color balance
